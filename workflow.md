---
description: 統合ワークフロー管理システムのマスターコントローラー
---

# 🎯 マスターワークフロー制御システム

プロジェクト作成を段階的に進める統合ワークフロー管理システムです。
ユーザーは各フェーズで質問に答え、承認を行いながら、要件定義から実装タスク分解まで完了させます。

## 🚀 クイックスタート

```bash
# 新規プロジェクト開始
/workflow "作りたいプロダクトの概要"

# 例
/workflow "中古品を販売できるECサイト"
/workflow "社内用のタスク管理ツール"
/workflow "不動産物件検索システム"
```

## 📋 コマンド一覧

### 基本コマンド

```bash
# 新規プロジェクト開始
/workflow "[プロジェクト概要]"

# 中断したプロジェクトの再開
/workflow --resume

# 現在の状態確認
/workflow --status

# 特定フェーズに戻る
/workflow --rollback [phase]

# ヘルプ表示
/workflow --help
```

### 管理コマンド

```bash
# プロジェクトリセット
/workflow --reset

# 状態ファイルの確認
/workflow --show-state

# 依存関係の表示
/workflow --show-dependencies

# デバッグモード
/workflow --debug
```

## 🔄 実行フロー

### 1. 新規プロジェクト開始時

```markdown
ユーザー: /workflow "中古品販売ECサイト"

Claude:
========================================
🚀 プロジェクト作成ワークフローを開始します
========================================

プロジェクト名: 中古品販売ECサイト
作成日時: 2024-01-25 10:00

📊 実行フェーズ:
1. 要件定義
2. 技術調査・分析
3. 詳細設計
4. タスク分解

[workflow-state.json を .claude/spec/ に初期化...]

========================================
📝 Phase 1: 要件定義
========================================

プロジェクトの要件を明確化します。

👤 **ユーザーアクション必須**
以下のコマンドを実行して要件定義を開始してください：
`/project-requirements`
```

### 2. 状態管理

#### workflow-state.json の構造

```json
{
  "project": {
    "name": "中古品販売ECサイト",
    "description": "個人間で中古品を売買できるECプラットフォーム",
    "type": "web_application",
    "created_at": "2024-01-25T10:00:00Z",
    "last_updated": "2024-01-25T14:30:00Z"
  },
  "workflow": {
    "current_phase": "analysis",
    "current_step": "questioning",
    "completed_phases": ["requirements"],
    "phase_history": [
      {
        "phase": "requirements",
        "status": "approved",
        "completed_at": "2024-01-25T11:00:00Z",
        "document": ".claude/spec/requirements.md"
      }
    ]
  },
  "responses": {
    "requirements": {
      "target_users": "個人および小規模業者",
      "main_features": ["商品出品", "購入・決済", "評価システム"],
      "constraints": {
        "budget": "500万円以内",
        "timeline": "3ヶ月",
        "technology": "クラウドネイティブ"
      }
    },
    "analysis": {
      "preferred_language": "Python",
      "database": "PostgreSQL",
      "infrastructure": "AWS"
    }
  },
  "documents": {
    "requirements": {
      "path": ".claude/spec/requirements.md",
      "status": "approved",
      "version": "1.0"
    },
    "design": {
      "path": ".claude/spec/design.md",
      "status": "draft",
      "version": "0.1"
    }
  }
}
```

### 3. 依存関係チェック

実行前に自動的に依存関係を検証：

```markdown
🔍 依存関係チェック中...

Phase: 詳細設計
必要条件:
✅ Phase 1: 要件定義 - 承認済み
✅ Phase 2: 技術調査 - 承認済み
✅ requirements.md - 存在確認
✅ tech-analysis.md - 存在確認

→ すべての条件を満たしています。実行を開始します。
```

### 4. フェーズ間の手動遷移

```markdown
✅ Phase 1: 要件定義が承認されました！

[状態を保存...]

👤 **次のユーザーアクション**
Phase 2を開始するには、以下のコマンドを手動で実行してください：
`/project-analysis`

⚠️ このコマンドを実行するまでワークフローは進行しません
```

## 🔐 エラーハンドリング

### 依存関係エラー

```markdown
⚠️ 実行できません

Phase 3: 詳細設計を開始しようとしましたが、
以下の前提条件が満たされていません：

❌ Phase 2: 技術調査 - 未承認（現在: questioning）

対処法:
1. `/workflow --resume` で中断位置から再開
2. `/workflow --status` で現在の状態を確認
```

### 状態不整合エラー

```markdown
⚠️ 状態ファイルに不整合が検出されました

検出された問題:
- current_phase と phase_history が一致しません

推奨アクション:
1. `/workflow --repair` で自動修復を試みる
2. `/workflow --reset` でプロジェクトをリセット
```

## 📊 進捗表示

### --status コマンド

```markdown
📊 プロジェクト進捗状況
========================

プロジェクト: 中古品販売ECサイト
開始日時: 2024-01-25 10:00
経過時間: 4時間30分

フェーズ進捗:
✅ Phase 1: 要件定義 ━━━━━━━━━━ 100%
🔄 Phase 2: 技術調査 ━━━━━━╸━━━  65%
⏸️ Phase 3: 詳細設計 ━━━━━━━━━━   0%
⏸️ Phase 4: タスク分解 ━━━━━━━━━━   0%

総合進捗: ████████░░░░░░░░░░░░ 40%

現在の作業:
- Phase 2の質問3/5を回答待ち
- 残り質問数: 2

次のアクション:
→ `/workflow --resume` で質問を再開
```

### 依存関係ビュー

```markdown
📊 依存関係マップ
================

requirements
    ↓
analysis ← 現在ここ
    ↓
design
    ↓
tasks → implementation

並行実行可能:
- design + test_planning
- documentation（any phase）

条件付きスキップ:
- prototype_mode: skip [formal_testing, full_documentation]
```

## 🎯 ベストプラクティス

### 1. 中断と再開

```bash
# 作業を中断する場合
Ctrl+C または ブラウザを閉じる
# → 状態は自動保存されます

# 再開する場合
/workflow --resume
# → 中断した位置から自動的に再開
```

### 2. 回答の修正

```bash
# 前のフェーズに戻って回答を修正
/workflow --rollback requirements

# 特定の質問だけ修正
/workflow --edit-response requirements.q3
```

### 3. 並行作業

```markdown
📌 並行実行可能なタスクが検出されました

以下のタスクを並行して実行できます：
1. 詳細設計
2. テスト計画作成

両方を並行実行しますか？ (yes/no/select)
```

## 🔧 カスタマイズ

### プロジェクトタイプ別テンプレート

```yaml
project_templates:
  web_application:
    default_tech_stack: "Python/Django/PostgreSQL"
    required_phases: ["requirements", "analysis", "design", "tasks"]
    optional_phases: ["prototyping", "user_testing"]

  mobile_app:
    default_tech_stack: "Flutter/Firebase"
    required_phases: ["requirements", "ui_design", "implementation"]
    skip_phases: ["server_setup"]

  scraping_system:
    default_tech_stack: "Python/BeautifulSoup/SQLite"
    required_phases: ["requirements", "target_analysis", "implementation"]
    focus: "data_extraction"
```

### 条件分岐ロジック

```yaml
conditional_flow:
  - condition: "project.type == 'prototype'"
    action: "skip"
    targets: ["formal_documentation", "comprehensive_testing"]

  - condition: "project.budget < 100万円"
    action: "use_template"
    template: "low_budget_optimization"

  - condition: "project.timeline < 1ヶ月"
    action: "enable"
    features: ["rapid_development_mode", "minimal_viable_product"]
```

## 📚 関連ドキュメント

- [ワークフロー詳細仕様](./refer/workflow-spec.md) - システムの詳細仕様書
- [workflow-dependencies.yaml](./workflow-dependencies.yaml) - 依存関係定義
- [各フェーズドキュメント](./project-*.md) - フェーズ別実行内容

## 🆘 トラブルシューティング

### よくある問題

1. **状態ファイルが見つからない**
   ```bash
   /workflow --init  # 状態ファイルを再初期化
   ```

2. **フェーズがスキップされる**
   ```bash
   /workflow --show-dependencies  # 依存関係を確認
   /workflow --force-run [phase]  # 強制実行（非推奨）
   ```

3. **無限ループになる**
   ```bash
   /workflow --break  # ループを強制終了
   /workflow --debug  # デバッグモードで原因調査
   ```

## 🎊 完了時の動作

すべてのフェーズが完了すると：

```markdown
🎉 プロジェクト作成が完了しました！
=====================================

プロジェクト: 中古品販売ECサイト
完了日時: 2024-01-25 18:00
総所要時間: 8時間

📁 生成されたドキュメント:
✅ requirements.md - 要件定義書
✅ design.md - 技術仕様書
✅ tasks.md - 実装タスク一覧
✅ architecture.md - アーキテクチャ設計書

📊 統計:
- 回答した質問数: 42
- 生成したドキュメント: 4
- 定義したタスク: 23

次のステップ:
1. `tasks.md` に従って実装を開始
2. `/implement --task 1` で最初のタスクから開始

お疲れ様でした！実装頑張ってください 🚀
```